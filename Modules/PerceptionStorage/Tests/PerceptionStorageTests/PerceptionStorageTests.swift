import XCTest
@testable import PerceptionStorage

final class PerceptionStorageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PerceptionStorage().text, "Hello, World!")
    }
}
